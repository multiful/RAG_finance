# 프로덕션·슬림 이미지 기본 (torch / ragas / llama-index / sentence-transformers 없음)
# 전체 기능(리랭크·Ragas·LlamaParse PDF API):  pip install -r requirements-full.txt
# Render 등 기존 풀 스택: build.sh 또는  pip install -r requirements-full.txt (constraints.txt 유지)
#
# Railway / CI에서 `pip install -r requirements.txt -c constraints.txt` 를 못 쓸 때:
#   1) Dockerfile 사용 시: 이미 PIP_CONSTRAINT=constraints.txt 로 설치함 (권장).
#   2) 환경 변수:  PIP_CONSTRAINT=constraints.txt  pip install -r requirements.txt
#   3) Nixpacks만 쓸 때: Build Command에 위와 동일한 한 줄을 넣거나, 아래에 이미 핀된 httpx/httpcore/scikit-learn 등을 requirements에 직접 유지.
# =========================
# FastAPI
# =========================
fastapi==0.109.2
uvicorn[standard]==0.27.1
python-multipart==0.0.9

# =========================
# Database / Supabase
# =========================
supabase==2.4.1
# httpx 0.28+ 는 AsyncClient(proxies=) 제거 — openai SDK 1.12와 맞추려면 0.27.x 고정
httpx==0.27.2
httpcore==1.0.5
psycopg2-binary==2.9.9
sqlalchemy==2.0.25
pgvector==0.2.4

# =========================
# Redis
# =========================
redis==5.0.1

# =========================
# OpenAI / LangChain
# =========================
openai==1.12.0
langchain==0.1.9
langchain-core>=0.1.27,<0.2
langchain-openai==0.0.8
langchain-community==0.0.21
langgraph==0.0.26
langsmith>=0.1.0,<0.2.0

# llama-index 0.10.x 와 맞춘 pydantic v2
pydantic==2.6.1
pydantic-settings==2.1.0

# =========================
# RSS / Scraping
# =========================
feedparser==6.0.11
requests==2.31.0
beautifulsoup4==4.12.3

# =========================
# 수치·로컬 PDF/HWP 폴백 (LlamaParse 미설치 시)
# =========================
numpy>=1.26.4,<3
pdfplumber>=0.11.0
olefile>=0.47

# =========================
# Utils
# =========================
aiofiles==23.2.1
aiohttp>=3.9.0,<4
python-dateutil==2.8.2
