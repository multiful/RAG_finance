# 슬림 requirements.txt 위에 추가 설치 (로컬 개발·평가·리랭크·LlamaParse API)
#   pip install -r requirements.txt -c constraints.txt
#   pip install -r requirements-full.txt -c constraints.txt
# =========================
# LlamaParse / LlamaIndex
# =========================
llama-parse==0.4.3
llama-index==0.10.14

# =========================
# RAG Evaluation
# =========================
ragas>=0.1.17
datasets>=2.14.0,<3

# =========================
# ML / Cross-Encoder 리랭크 (torch 포함 — CPU 휠 권장)
# =========================
scikit-learn==1.4.2
pandas>=2.2.1,<3
sentence-transformers>=2.2.2
